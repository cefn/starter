import { say } from "../src";

test("This is not a test", () => {
  say("nothing");
});

test("Checking something else", () => {
  say("nothing more");
});

test("Checking something further", () => {
  // expect(true).toBe(false);
});
